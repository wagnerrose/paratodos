$(document).on("change", "#states_select", function(){
  var country = $(this).val();

  $.ajax({
    url: "site/analyzes/index",
    method: "GET",
    dataType: "json",
    data: {state: state},
    error: function (xhr, status, error) {
      console.error('AJAX Error: ' + status + error);
    },
    success: function (response) {
      console.log(response);
      var counties = response["counties"];
      $("#counties_select").empty();

      $("#counties_select").append('<option>Selecione Localidade</option>');
      for(var i=0; i< counties.length; i++){
        $("#counties_select").append('<option value="' + counties[i]["id"] + '">' + cities[i]["name"] + '</option>');
      }
    }
  });
});
